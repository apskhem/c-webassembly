mod {
    mem memory = (1; page; 2);
    tab table = (1; fref; 100);
    
    type BinaryFunction = fn(i32, i32) -> i32;

    // testing function
    fn test(a: i32, b: i32) -> (i32; 2) {
        if (a) {
            lc l0 <- a;
        }
        elif (a) {
            while (a < b) {
                break;
            }
        }
        else {

        }

        test();

        lc indirect_call_test <- *10(table)::<BinaryFunction>(10, er, t, 3 + 1.2);
        lc int_test <- 1234;
        int_test <- I32.add(1, 3) + (-1);
        ret -10.1;
    }
}
